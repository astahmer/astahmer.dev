---
import { TabItemTagname } from './tabs-rehype'

interface Props {
  label: string
}

const { label } = Astro.props

if (!label) {
  throw new Error('Missing prop `label` on `<TabItem>` component.')
}
---

<TabItemTagname data-label={label}>
  <slot />
</TabItemTagname>
